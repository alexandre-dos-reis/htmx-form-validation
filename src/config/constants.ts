export const HX_CONSTANTS = {
  "App-Form-Validation": {
    key: "App-Form-Validation",
    attribute: {
      "hx-headers": '{"App-Form-Validation": "true"}',
    },
  },
} as const;

export const ATTRIBUTES_CONSTANTS = {
  form: {
    inputWrapperId: `-input-wrapper`,
    inputErrorId: `-input-error`,
    inputId: `-input-field`,
  },
} as const;
